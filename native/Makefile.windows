CXX = x86_64-w64-mingw32-g++
CXXFLAGS = -std=c++17 -O2 \
           -I./include \
           -I./include/godot_cpp \
           -I./include/gdextension \
           -DWINDOWS_ENABLED -DGDEXTENSION -DSPACEWHEAT_WITH_GODOT \
           -pthread

LDFLAGS = -shared -static-libgcc -static-libstdc++ \
          ./lib/libgodot-cpp.windows.template_release.x86_64.a \
          -pthread

SOURCES = $(wildcard src/*.cpp) $(wildcard src/*/*.cpp)
OBJECTS = $(SOURCES:.cpp=.obj)
DEPFILES = $(OBJECTS:.obj=.d)
TARGET = bin/windows/libquantummatrix.windows.template_release.x86_64.dll
DEBUG_TARGET = bin/windows/libquantummatrix.windows.template_debug.x86_64.dll

all: $(TARGET) $(DEBUG_TARGET)

$(TARGET): | bin/windows

bin/windows:
	mkdir -p bin/windows

$(TARGET): $(OBJECTS)
	$(CXX) $(OBJECTS) -o $@ $(LDFLAGS)
	@echo ""
	@echo "✓ Windows build complete: $(TARGET)"
	@ls -lh $(TARGET)

$(DEBUG_TARGET): $(TARGET)
	rm -f $(DEBUG_TARGET)
	cp $(TARGET) $(DEBUG_TARGET)

%.obj: %.cpp
	@echo "Compiling $< for Windows..."
	$(CXX) $(CXXFLAGS) -MMD -MP -c $< -o $@

clean:
	rm -f src/*.obj src/*/*.obj src/*.d src/*/*.d bin/windows/*.dll

.PHONY: all clean

-include $(DEPFILES)

# P7 — the sweep. Coordinator's flight plan.

The Country Chapters pass ends here: fresh checkpoints on final data (P7a,
agent-minted), then sensor legs prove each chapter is walkable by a
haiku-class player under the fail-fast protocol, five branch legs prove the
fork, and the E2 gamma ladder rides along. Every leg's claim is verified
against manifests; every wall fixes a path, never a tester.

## Prerequisites (P7a, in flight)
Checkpoints on 5d77d86c: act1_complete / act2_woodlot_taught / act2_complete /
act3_complete / act4_hub — each with .flag_progress.json pinned expectations
and .witness_gauge.json banked.

## P7b — chapter proof legs (haiku sensors)

One leg per chapter, each from its checkpoint, objective = the NEXT chapter's
terminal flag:

| leg | from | must reach | teaches the sensor |
|---|---|---|---|
| L1 | fresh boot | lumber_flows | act 0-1 + woodlot door/contact by contracts |
| L2 | act2_woodlot_taught | pond_breathes | plant ritual + spring chapter (trust lane) |
| L3 | act2_complete | chain_flipped | mill + Lanternfall witness contact (the "circuits" chapter — the owner's open haiku-difficulty question; a wall here is a DESIGN ANSWER, not a failure) |
| L4 | act3_complete | five_doors + both teachers | the hub, one deliberate word |
| L5..L7 | act4_hub | one village_path_* each (industrial/watched/cemetery) | trim + plant one door key |

P7a FINDING (design, for the owner): commons + artisan fire AT the five_doors
cascade — their key atoms (💧, 🔨) live in Village by construction (pond
DELIVER, mill plant). The fork's real choices are the other three doors.
Whether commons/artisan should demand a deliberate plant is an owner call
(the composition-IS-character fiction argues both ways); until ruled, the
branch proof is 3 legs + 2 organically-proven paths.

Sensor orders (every leg): screen text + seat verbs only; NEVER edit files;
~8-10 presses without visible progress on the current objective → bank, file
tried/saw/expected, STOP (`hive.py wall <chapter> "<report>"`). Early
surrender is a success. Claims verified against manifest deltas — haiku
reports are hypotheses, not facts.

## E2 gamma ladder (rides P7b)

Chapter legs L1/L2/L4 fly 3× — WITNESS_GAMMA_SCALE ∈ {0.25, 1.0, 4.0} in the
seat env, same checkpoint + press budget per rung; bank each. L3 and branch
legs fly 1.0 only (walls there must not confound the experiment). Score:
`gamma_ab.py report`. Never compare across chapters (surprise is
cadence-relative). Pre-registered: 0.25× raises decisiveness; the question is
its surprise cost.

## Ledger per leg (coordinator duties, hive protocol laws 4-6)
1. Verify claim vs manifest → `hive.py leg <chapter> --claim c --truth t --accurate a`
2. Wall? → escalate to a design-level path repair (builder), audit it, THEN re-fly.
3. `hive.py audit --claim ... --checked ...` after each audited landing.

## Post-sweep (before sprint report)
- braid_alphabet arc asks ×6 vs flag's 4 — fix arc to mirror the flag (arcs
  mirror gates; template law). BLOCKED until checkpoints are minted (story
  data frozen during P7a).
- village_identity calibration pinned from act4_hub.flag_progress.json.
- act3_5 end-to-end once more on final data; full suite; then the #265
  verdict: can a haiku beat the whole game?

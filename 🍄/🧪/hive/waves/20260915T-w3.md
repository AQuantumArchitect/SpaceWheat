# Wave 2026-09-15 — four sensor personas, Act 0 fresh

Humans: this page. Agents: `../prompts/`.

| Persona | Presses | Outcome | Note |
|---|---|---|---|
| literalist | 3 | wall | mill ask names **tap**, not a key |
| earnest | 11 | wall | same mill board; g/enter/space failed |
| lost-lamb | 10 | wall (DRIFT+LOOPING) | same mill ask |
| masher | 20 | plateau | still "Strike a sleeping plot"; contained |

Harness (wave 1, discarded): four parallel Godots STALE; `start` was not session-detached. Fixed: `rig_client.start_listener(..., start_new_session=True)`, `--jobs 1`, `hive.py wall --file`.

Claude CLI `auth status` = logged out. Legs ran grok-4.5 as sensor-class. Relogin then `python3 🍄/🧪/hive/send_wave.py --chapter act0_fresh`.

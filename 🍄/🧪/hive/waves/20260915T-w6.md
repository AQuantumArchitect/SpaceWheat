# Wave 6 — grok-4.5, Act 0 fresh, after Superpose banner named `[E]`

Humans: this page. Agents: `../prompts/`.

Model: grok-4.5. Sequential seats. Path repair vs wave 5: `_verb_line` names `[E] Superpose` / `[R] Weave` once travel is empty.

| Persona | Presses | Outcome | Note |
|---|---|---|---|
| literalist | 7 | wall | **C + ESC + U followed.** Then dual-`[E]`: banner Superpose vs Ace Pause. Immediate literalist wall (same-key-two-jobs). Path `r f r c r escape u`. |
| lost-lamb | 12 | wall (LOOPING) | Same path then `e` (auto-wore Druid, also paused). Reached Bell Gate UI. `[Q] Select 2+ qubits` is disabled — Q does not select. `[R] Weave` while R is locked. |
| earnest | 20 | wall | Superpose via Tab → Druid → E (H-Gate). Two forest plots H-gated. Gate UI: Select not available; Inspect needs 1🔬 have 0. |
| masher | 20 | plateau | Contained on Strike. No crash. |

Mill + forest-cross still keyboard-followable. Superpose *key* is named but collides with Pause. Bell *key* is named but Q's "Select" is a lie.

Path repair for wave 7: hat-first banner (`[0] Druid` then `[E]`; `[9] Operator` then `[R]`); Gate empty chip names Shift-click / Shift+G H.

Harness: umweltd down; local walls.jsonl is record. One Godot.

# Wave 7 — grok-4.5, Act 0 fresh, hat-first Superpose + ESC-only travel

Humans: this page. Agents: `../prompts/`.

Model: grok-4.5. Sequential seats. Path repair vs wave 6: banner names `[0] Druid` until worn, then `[E] Superpose` (no dual-Pause); menu-open travel names **only** `ESC closes` (no leftover `[U]`); Gate empty chip names Shift-click / Shift+G H.

| Persona | Presses | Outcome | Note |
|---|---|---|---|
| literalist (w7lite) | 5 | wall | first fly: board still printed `[U] COMMITMENTS` next to `ESC then [U]`. Discarded. Tightened to ESC-only. |
| literalist (w7blite) | 11 | wall | **Superpose followed.** Path `r f r c r escape u 0 e 9 r`. Bell Gate UI: `[R] Weave` vs `[R] -`; Q is Shift-click not a single key. |
| lost-lamb | 16 | wall (LOOPING) | Same Superpose path `… 0 e 9 r`, then `shift+g` looping — no check feedback, banner still `[R] Weave`. |
| earnest | 20 | wall | Superpose `[0]` then `[E]` **yes**. Bell: Shift+G H + Weave stuck on `needs 1🔬 (have 0)`. |

Mill, forest-cross, and Superpose are now keyboard-followable. Next door: **Bell pair** (check two plots with visible feedback; Weave gated on 🔬 the wallet does not have).

Harness: umweltd down; local walls.jsonl is record. One Godot. No masher this wave (wave 6 masher already contained).

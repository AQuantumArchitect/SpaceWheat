# Godot project files
.godot/
*.translation

# LLM working directories (not source code)
llm_inbox/
llm_outbox/

# Archive directories (deprecated code)
Archive/
archive/

# Test workspace (large local scratch area)
Tests/

# Editor-specific files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build and export
bin/
build/
export/
exports/
releases/
*.exe
*.dll
*.so
*.dylib
*.a
*.o
*.obj
*.lib
*.pdb

# Native build artifacts
native/bin/
!native/bin/
!native/bin/linux/
!native/bin/windows/
!native/bin/linux/libquantummatrix.linux.template_release.x86_64.so
!native/bin/windows/libquantummatrix.windows.template_release.x86_64.dll
!native/bin/windows/libquantummatrix.windows.template_debug.x86_64.dll
native/lib/*.a
native/lib/*.so
native/src/*.o
native/src/*.obj
native_OLD_BLOATED_*/
*.gd.backup
*.gd.old
*.gd.old_v1

# User-specific
*.cfg
!export_presets.cfg  # Need this for building on other machines
user_settings/
local_settings.json

# Python cache
__pycache__/
*.py[cod]
*$py.class

# Temporary files
*.tmp
*.tmp.*
*.gd.tmp.*
*.bak
*.log
logs/

# WSL artifacts
*Zone.Identifier

# Test outputs
test_results/
player_logs/
llm_inbox/player_logs/

# Godot shader cache (auto-generated)
.godot/shader_cache/

# Keep tracked: Source code, scenes, and documentation
# Do not ignore:
# - project.godot
# - .gd files
# - .tscn files
# - .tres files
# - .md files
# - .sh files
# - .py files (except __pycache__)
build_web/

# 🍄 runtime artifacts (ignore by default)
🍄/logs/
🍄/🎛️/logs/
🍄/🎛️/🧾/
🍄/🎛️/.godot/
🍄/🎛️/.godot_tmp/
🍄/🎛️/.godot_tmp2/
🍄/🎛️/__pycache__/
🍄/🎛️/dev/__pycache__/

# 🍄 curated artifacts explicitly kept in git
!🍄/artifacts_whitelist/
!🍄/artifacts_whitelist/**
📦_emoji_research/
.venv/

# Transient rig/build scratch (runtime artifacts, not source)
.build/
.godot_windows_rig/

# Rig screenshot artifacts
🍄/🧪/demo_shots/
🍄/🧪/playthrough_shots/

# Minted checkpoint banks (runtime artifacts; re-mintable from the drives) —
# except the two shipped endgame fixtures tests/test_field_shows_the_right_country.py
# needs on a fresh clone (real campaign saves, re-mintable via act3_5_drive.py
# ACT35_RESUME=1 ACT35_ENDGAME=1, not hand-authored). Ignore CONTENTS with `*`,
# not the directory itself — negating the directory node makes git traverse in
# and expose every OTHER checkpoint file as untracked too, not just these four.
🍄/🧪/checkpoints/*
!🍄/🧪/checkpoints/endrun_act7.tres
!🍄/🧪/checkpoints/endrun_act7.json
!🍄/🧪/checkpoints/endrun_act8.tres
!🍄/🧪/checkpoints/endrun_act8.json
node_modules/
native/src/*.d
native/src/**/*.obj
scratch_cognifold_sample.json

# Written by scripts/build-desktop-local.sh immediately before an export and
# deleted immediately after — it belongs to a pack, never to the tree.
Core/Config/build_stamp.json
Core/Config/build_stamp.json.uid
